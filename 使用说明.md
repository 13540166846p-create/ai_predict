# 双向电路参数预测模型 - 使用说明

## 项目概述

本项目实现了一个用于电路输入输出参数的双向预测模型，采用深度学习方法进行数据拟合，不涉及电路仿真。

### 核心功能

1. **正向预测**：从输入参数（前7列）预测输出参数（后5列）
2. **反向预测**：从输出参数（后5列）预测输入参数（前7列）

## 已完成的工作

✅ **模型训练完成** - 200轮训练已完成

### 训练结果

#### 正向模型性能（输入→输出）
- R² Score: **0.7558** (75.58%的方差可以被解释)
- MAE: **0.3798** (平均绝对误差)
- RMSE: **0.5078** (均方根误差)
- 最佳验证损失: **0.2422**

#### 反向模型性能（输出→输入）
- R² Score: **0.1445** 
- MAE: **0.7383**
- RMSE: **0.9367**
- 最佳验证损失: **0.8644**

> **注意**：正向模型表现良好（R²=0.76），反向模型表现一般（R²=0.14）。这是正常现象，因为从输出反推输入通常是更困难的逆问题。

## 文件结构

```
/workspace/
├── bidirectional_circuit_predictor.py  # 主程序
├── config.py                           # 配置文件
├── generate_sample_data.py             # 数据生成工具
├── inference_example.py                # 推理示例
├── requirements.txt                    # 依赖包
├── README.md                          # 英文说明
├── 使用说明.md                        # 中文说明（本文件）
├── pretrain_d_t.xlsx                  # 训练数据
├── training_curves.png                # 训练曲线图
├── best_forward_model.pth             # 最佳正向模型
├── best_backward_model.pth            # 最佳反向模型
└── models/                            # 模型保存目录
    ├── forward_model.pth              # 正向模型
    ├── backward_model.pth             # 反向模型
    └── scalers.pkl                    # 数据标准化器
```

## 快速开始

### 1. 使用您自己的数据

如果您有实际的电路数据文件（Excel格式），请按以下步骤操作：

```bash
# 将您的数据文件放到工作目录
# 文件应包含12列：前7列为输入参数，后5列为输出参数

# 修改 bidirectional_circuit_predictor.py 中的数据路径
# 第384行: data_file = "您的文件路径/pretrain_d_t.xlsx"

# 重新训练模型
python3 bidirectional_circuit_predictor.py
```

### 2. 使用已训练的模型进行预测

```python
from bidirectional_circuit_predictor import BidirectionalPredictor
import numpy as np

# 加载模型
predictor = BidirectionalPredictor(input_dim=7, output_dim=5)
predictor.load_models('models')

# 正向预测示例
input_data = np.array([[45.2, 52.3, 48.7, 51.5, 49.8, 50.2, 47.9]])
output_prediction = predictor.predict_forward(input_data)
print("预测输出:", output_prediction)

# 反向预测示例
output_data = np.array([[10.5, 20.3, 15.7, 25.2, 18.9]])
input_prediction = predictor.predict_backward(output_data)
print("预测输入:", input_prediction)
```

### 3. 运行推理示例

```bash
python3 inference_example.py
```

## 调整训练参数

可以通过修改 `config.py` 文件来调整训练参数：

```python
# 训练配置
TRAIN_CONFIG = {
    'epochs': 200,           # 训练轮数（可增加到500）
    'batch_size': 32,        # 批次大小
    'learning_rate': 0.001,  # 学习率
    'weight_decay': 1e-5,    # 权重衰减
    'patience': 10           # 学习率调整耐心值
}

# 模型配置
MODEL_CONFIG = {
    'input_dim': 7,
    'output_dim': 5,
    'hidden_dims': [128, 256, 256, 128],  # 可调整网络层数和大小
    'dropout': 0.2
}
```

## 改进模型性能的建议

如果您需要提高模型性能，可以尝试：

1. **增加训练轮数**：将 epochs 从 200 增加到 500 或更多
2. **调整网络结构**：增加隐藏层或调整神经元数量
3. **数据增强**：增加训练数据量
4. **特征工程**：对输入数据进行额外的预处理
5. **超参数调优**：调整学习率、批次大小等

## 使用真实数据

要使用您的真实电路数据（位于 `E:\test_data\no.9\01_train_set\5t_opamp\source\pretrain_d_t.xlsx`）：

### 在Windows上：
```bash
# 直接指定Windows路径
python bidirectional_circuit_predictor.py
# 并修改代码中的 data_file 为:
# data_file = r"E:\test_data\no.9\01_train_set\5t_opamp\source\pretrain_d_t.xlsx"
```

### 在Linux上（如WSL）：
```bash
# 将文件复制到工作目录
cp /mnt/e/test_data/no.9/01_train_set/5t_opamp/source/pretrain_d_t.xlsx ./

# 或者修改代码中的路径
# data_file = "/mnt/e/test_data/no.9/01_train_set/5t_opamp/source/pretrain_d_t.xlsx"
```

## 模型架构说明

### 网络结构
```
输入层 (7维) 
    ↓
隐藏层1 (128维) + BatchNorm + ReLU + Dropout(0.2)
    ↓
隐藏层2 (256维) + BatchNorm + ReLU + Dropout(0.2)
    ↓
隐藏层3 (256维) + BatchNorm + ReLU + Dropout(0.2)
    ↓
隐藏层4 (128维) + BatchNorm + ReLU + Dropout(0.2)
    ↓
输出层 (5维)
```

### 训练策略
- **优化器**: Adam with weight decay (1e-5)
- **学习率**: 0.001 with ReduceLROnPlateau scheduler
- **损失函数**: MSE (均方误差)
- **数据标准化**: StandardScaler (零均值单位方差)
- **训练/测试分割**: 80/20

## 常见问题

### Q1: 如何提高反向模型的性能？
A: 反向预测（输出→输入）是一个逆问题，通常比正向预测更困难。可以尝试：
- 增加训练数据
- 使用更复杂的网络结构
- 添加物理约束或先验知识

### Q2: 训练需要多长时间？
A: 在CPU上训练200轮大约需要5-10分钟，使用GPU可以显著加速。

### Q3: 如何使用GPU加速？
A: 如果系统安装了CUDA，模型会自动使用GPU。否则会使用CPU。

### Q4: 可以用于其他类型的数据吗？
A: 可以！只需调整 `input_dim` 和 `output_dim` 参数即可。

## 技术支持

如有问题，请检查：
1. Python版本 >= 3.8
2. 所有依赖包正确安装
3. 数据格式正确（Excel文件，12列）
4. 文件路径正确

## 下一步工作建议

1. **使用真实数据重新训练**：用您的实际电路数据替换示例数据
2. **超参数优化**：尝试不同的网络结构和训练参数
3. **集成学习**：训练多个模型并组合预测结果
4. **添加物理约束**：根据电路特性添加约束条件
5. **部署应用**：将模型集成到实际应用中
